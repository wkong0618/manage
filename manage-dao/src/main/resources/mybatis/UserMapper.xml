<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wk.manage.dao.mapper.UserMapper">
    <update id="ageAdd">
        update user set  age = age + #{add} where id = #{id}
    </update>

    <update id="ageAdd1">
        update user set  age = age + #{add} where id = #{id1}
    </update>
</mapper>